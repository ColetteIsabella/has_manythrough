<% name(@user.name, "Users") %>
<h2><%= @user.name %></h2>

<%= link_to "Edit User",
    edit_user_path(@user) %>

<%= link_to "Delete User",
    user_path(@user),
    :method => :delete,
    :confirm => "Are you sure you wish to delete this user?" %>

<!-- <ul id="phases">
  <% @user.phases.each do |phase| %>
    <li>
      #<%= phase.id %> -
      <%= link_to phase.title, [@user, phase] %>
    </li>
    <% end %>
  </ul> -->
